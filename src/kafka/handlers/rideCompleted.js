
async function rideCompletedHandler({ message }) {
    // const id = JSON.parse(message.value.toString().trim());

    // if (!id) {
    //     console.error('Invalid message: ID is missing');
    //     return;
    // }
}

export default rideCompletedHandler;